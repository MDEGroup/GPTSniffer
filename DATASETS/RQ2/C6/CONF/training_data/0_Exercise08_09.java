import java.util.Scanner;public class TicTacToe {  public static void main(String[] args) {    char[][] board = new char[3][3];    initializeBoard(board);    displayBoard(board);    char currentPlayer = 'X';    boolean gameEnded = false;    int row, col;    while (!gameEnded) {      System.out.printf("Enter a row (0, 1, or 2) for player %s: ", currentPlayer);      row = getPlayerInput();      System.out.printf("Enter a column (0, 1, or 2) for player %s: ", currentPlayer);      col = getPlayerInput();      if (board[row][col] == '\u0000') { 